import React from 'react'

function User() {
  //${voteFourPer == 0 ? 'static': 'absolute'}
  //${voteThreePer == 0 ? 'static': 'absolute'}
  //${voteTwoPer == 0 ? 'static': 'absolute'}
  //${voteOnePer == 0 ? 'static':'absolute'}
  return (
    <div>User</div>
  )
}

export default User